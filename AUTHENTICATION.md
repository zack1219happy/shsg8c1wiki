# 认证规则

## 登录

- 登录框支持输入姓名或用户名，按精确文本匹配，前后空格会被去掉。
- 未设置自定义密码的账号使用学号登录；已设置自定义密码的账号使用当前自定义密码登录。
- 数据库密码校验通过后，还必须建立与目标账号 ID 一致的 Supabase Auth 会话，页面才会显示登录成功。
- Auth 会话失效、账号记录不存在或两边账号 ID 不一致时，会清除本地登录状态。

## 账号修改

- 密码和用户名修改只能作用于当前认证用户。
- 修改密码时先验证当前密码，再更新认证密码和应用密码；同步失败会明确提示，不会报告为成功。

## 登录保护

- IP 白名单启用时，缺失或无法解析客户端 IP 会拒绝登录。

## 数据库部署

线上需要同时存在 `login`、`get_current_user` 和 `verify_password` 规则。`db/migrations/20260904_login_hardening.sql` 是可版本控制的迁移入口；`schema.sql` 是本地数据库快照，已按线上规则同步。部署时应保留这些函数的权限设置，登录函数允许匿名调用，当前用户和密码函数只允许已认证用户调用。
